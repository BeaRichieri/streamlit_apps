The synthetic observations were generated from the reservoir model with

$$
k=0.18,\qquad b=1.15,
$$

and then a small amount of random observation noise was added.

In a real application these generating parameter values would be unknown. They are shown here only because this is a teaching experiment. Importantly, the noisy observations can also be reproduced very well by other parameter combinations.
