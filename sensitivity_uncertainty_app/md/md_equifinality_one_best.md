A conventional optimization can return the parameter combination with the highest value of a selected performance criterion.

This is useful, but it gives only **one point** in parameter space and can hide other combinations with almost the same performance.
