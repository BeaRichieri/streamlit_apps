At small $N$, the numerical estimates can still differ noticeably from the exact reference values. As $N$ increases, the estimates become more stable and approach the exact lines.

The improved stability has a computational cost. With three parameters, the estimator requires

$$
N(k+2)=5N
$$

model realizations, so doubling $N$ also doubles the number of model evaluations.
