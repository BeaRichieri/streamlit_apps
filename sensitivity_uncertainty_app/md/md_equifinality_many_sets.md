An uncertainty perspective asks whether **several different parameter combinations** are also compatible with the observations.

If many such solutions exist, the parameter values remain uncertain even when the model fit is good.
