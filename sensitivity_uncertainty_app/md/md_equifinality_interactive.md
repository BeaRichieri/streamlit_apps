Two parameter sets are shown below. Change the values of $k$ and $b$ and compare both the simulated hydrographs and their KGE values.

The default values are deliberately chosen so that **set A and set B are quite different in parameter space but both reproduce the observations very well**.
