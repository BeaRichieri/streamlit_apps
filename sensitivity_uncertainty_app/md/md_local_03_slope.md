At the current reference point, **[[PARAMETER]] = [[REFERENCE_VALUE]]**.
The code uses a **[[METHOD]]** because of the position of the reference point
relative to the parameter bounds.

The two orange points are located at

$$
[[PARAMETER]]=[[X_A]]
\qquad \text{and} \qquad
[[PARAMETER]]=[[X_B]].
$$

The dashed line connecting them has slope

$$
S_{[[INDEX]]} \approx [[SLOPE]].
$$
