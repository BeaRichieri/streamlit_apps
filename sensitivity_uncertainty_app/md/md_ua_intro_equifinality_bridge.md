Sampling plausible parameter values and running the model repeatedly produces an **ensemble** of model realizations. The spread of the ensemble shows how parameter uncertainty can propagate into prediction uncertainty.

Observations can help constrain this ensemble, but they do not necessarily identify one unique parameter set. Different combinations of parameter values may produce very similar model responses.

The next page explores this idea of **equifinality** interactively before introducing GLUE.
