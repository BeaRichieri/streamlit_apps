Instead of looking only at two parameter sets, we can evaluate model performance across the full $(k,b)$ parameter space.

The contour plot below shows the KGE obtained for many combinations of $k$ and $b$. The markers indicate your current parameter sets A and B.

Look at the **shape of the high-performance region** rather than only at the single highest point.
