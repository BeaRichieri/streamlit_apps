Keep set A fixed. Move set B through combinations such as:

- $k\approx0.25$, $b\approx1.04$;
- $k\approx0.35$, $b\approx0.92$.

Watch set B on the parameter-space plot.

**Question:** Why can increasing $k$ while decreasing $b$ still produce a good fit?
