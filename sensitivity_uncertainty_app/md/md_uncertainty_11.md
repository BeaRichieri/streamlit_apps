An uncertainty analysis can retain **multiple parameter sets** that reproduce
the observations sufficiently well.

The resulting ensemble makes it possible to investigate the uncertainty in
both the parameters and the model predictions.

**Robust model predictions!**
