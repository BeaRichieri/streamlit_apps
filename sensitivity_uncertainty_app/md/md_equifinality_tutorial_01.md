Start from the default values:

- set A: approximately $k=0.18$, $b=1.15$;
- set B: approximately $k=0.315$, $b=0.96$.

Compare the two parameter sets, their KGE values and the simulated hydrographs.

**Question:** Can two clearly different parameter combinations reproduce the same observations almost equally well?
