The same precipitation input is used for every model realization. Only the uncertain parameter values change between runs.

The synthetic experiment contains **80 time steps** and **11 precipitation events**. The initial reservoir storage is fixed at **5.0**.
