A local sensitivity describes the slope of the model response **at one selected
reference point**. To see this more clearly, inspect one parameter at a time.

For the selected parameter, the plot below varies only that parameter between
0 and 1. The other two parameters remain fixed at their current reference
values. The **black point** marks the current reference value, while the two
orange points show the locations used to estimate the finite-difference slope.
