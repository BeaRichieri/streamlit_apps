Synthetic observations are generated from the same reservoir model using a reference parameter set and then adding a small amount of random noise.

This gives us observations against which every sampled model realization can be evaluated.
