A **deterministic calibration** may identify the **single parameter set** that
maximizes or minimizes a chosen objective function.

This provides one best-performing solution for that criterion, but does not by
itself describe the range of other parameter sets that may also be plausible. 

**Correct output for the wrong reason!**
