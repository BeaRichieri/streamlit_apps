Select one parameter in the convergence plot and compare small and large values of $N$.

- How do the estimated $S_i$ and $S_{T_i}$ change as $N$ increases?
- What happens to the number of model realizations?
