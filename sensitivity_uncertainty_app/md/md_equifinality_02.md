In parameter estimation we often compare model simulations with observations and search for parameter values that give good model performance.

However, a high model-performance value does **not** necessarily mean that the corresponding parameters are uniquely determined. Another parameter combination may produce a very similar response.

This situation is called **equifinality**.
