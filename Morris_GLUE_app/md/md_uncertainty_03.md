Sensitivity analysis showed us **which parameters influence the model output**.
Parameter uncertainty addresses a different question: **what happens when the
values of those parameters are themselves uncertain?**
