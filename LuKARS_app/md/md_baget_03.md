The page loads the predefined dataset from `assets/input_Baget.txt`.

The required precipitation column is named `P`. Observed spring discharge is read from `QobsS`. Missing discharge observations remain missing and are excluded only from metric calculations.
