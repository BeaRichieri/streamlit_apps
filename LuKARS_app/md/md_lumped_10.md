Conceptual models range from simple single-reservoir structures to complex networks of interacting compartments.

A more complex structure can represent a larger variety of hydrological processes. However, every additional compartment or process normally introduces further parameters that must be estimated from the available observations.

A model should therefore be **complex enough to represent the dominant system behaviour, but not more complex than the available data can support**.
